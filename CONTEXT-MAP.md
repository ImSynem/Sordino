# Context Map

This repo uses a multi-context layout. Each context has its own
`CONTEXT.md`; system-wide architectural decisions live in `docs/adr/`.

| Context | Path | Status |
| ------- | ---- | ------ |
| _(none defined yet)_ | `src/<context>/CONTEXT.md` | pending |

Contexts are added lazily by `/grill-with-docs` as domain language
and boundaries emerge.
